# Routes,"desc" VARCHAR(254) NOT NULL
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing how to write asynchronous code
GET     /records/:id                controllers.RecordsController.record(id : String)
POST    /records                    controllers.RecordsController.insertRecord
